/* import 'package:dio/dio.dart'; */
import 'package:http/http.dart' as http;
/* import 'package:flutter_test/flutter_test.dart'; */
import 'package:mockito/annotations.dart';
/* import 'package:mockito/mockito.dart';
import 'package:wallet_crypto/service/crypto_service.dart'; */

import 'api_test.mocks.dart';

@GenerateMocks([http.Client])
void main() {
  /* group('fetchCrypto', () {
    test('fetchData returns data if the HTTP call completes successfully',
        () async {
      final Dio dio = Dio();
      dio.
      final client = MockClient();
      when(client.get(Uri.parse('https://api.coincap.io/v2/assets'))).thenAnswer((_) async => http.Response('', 200));
      expect(await fetchCrypto(client), matcher)
    });
  }); */
}
